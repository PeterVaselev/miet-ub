Написано только под линукс системы. Для билда написать `make`.

В докладе не было кода vector_test.cpp, однако он показывает что современный .at() даже быстрее индексирования по [], однако как только мы укажем параметр volatile, то вариант с UB всё ещё быстрее.
